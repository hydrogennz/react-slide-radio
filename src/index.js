import React from 'react';
import { render } from 'react-dom';
import SlideRadioExample from './SlideRadioExample';

const mountNode = document.getElementById('slide-radio-input');

render(<SlideRadioExample />, mountNode);